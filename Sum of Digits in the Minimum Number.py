class Solution:
    def sumOfDigits(self, A: List[int]) -> int:
        if not A:
            return 0
        n = min(A)
        if n:
            ans=0
            n=str(n)
            for i in n)):
                #ans+=int(i)
                print(int(i))
                